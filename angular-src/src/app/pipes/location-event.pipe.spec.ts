import { LocationEventPipe } from './location-event.pipe';

describe('LocationEventPipe', () => {
  it('create an instance', () => {
    const pipe = new LocationEventPipe();
    expect(pipe).toBeTruthy();
  });
});
